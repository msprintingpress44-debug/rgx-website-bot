# RGX Website And Telegram Bot

Static website:

- `index.html` is the public RGX landing page.
- `download.html?id=<file-id>` is the countdown download page.
- `data/files.json` is updated by the Telegram bot when admin generates links.

Telegram bot:

- See `Telegram bot/README.md`.
- Website admin routes are intentionally not included.
